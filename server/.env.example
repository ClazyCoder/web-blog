# JWT Secret Key (프로덕션에서 반드시 변경하세요)
SECRET_KEY=your-super-secret-jwt-key-change-this-in-production

# 관리자 계정 정보
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-secure-admin-password
ADMIN_EMAIL=admin@example.com

# 데이터베이스 설정 (비동기)
# SQLite (개발 환경)
DATABASE_URL=sqlite+aiosqlite:///./blog.db

# PostgreSQL (프로덕션 권장, asyncpg 사용)
# DATABASE_URL=postgresql+asyncpg://username:password@localhost:5432/blog_db

# MySQL (aiomysql 사용)
# DATABASE_URL=mysql+aiomysql://username:password@localhost:3306/blog_db

# Redis (캐시, Rate Limiting, 토큰 블랙리스트)
# 설정하지 않으면 인메모리 폴백 (개발 환경)
# REDIS_URL=redis://localhost:6379/0

# JWT 토큰 만료 시간 (분)
# ACCESS_TOKEN_EXPIRE_MINUTES=30

# 서버 설정
# HOST=0.0.0.0
# PORT=8000

# CORS 허용 origin (프로덕션 도메인 추가)
# CORS_ORIGINS=http://localhost:5173,https://yourdomain.com
