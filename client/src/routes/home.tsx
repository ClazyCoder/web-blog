import React from 'react';
import { BoardLayout } from "../layouts";

const Home: React.FC = () => {
    return (
        <div>
            <BoardLayout />
        </div>
    );
};

export default Home; 